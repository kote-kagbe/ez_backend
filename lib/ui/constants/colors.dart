import 'package:flutter/material.dart';

const systemColorValue = 0xff2c3e50;
const systemErrorColorValue = 0xffc2140a;

const systemColor = Color(systemColorValue);
const systemErrorColor = Color(systemErrorColorValue);
